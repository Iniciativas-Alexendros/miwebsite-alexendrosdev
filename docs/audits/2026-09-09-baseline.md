# Baseline audit — 2026-09-09

Auditoría WP-0 del MVP `miwebsite-alexendrosdev`. **Sin cambios de comportamiento de producción.**  
Repo: `Iniciativas-Alexendros/miwebsite-alexendrosdev`. Dominio: `https://alexendros.dev`. Proyecto Vercel: `alexendros-dev`.

## Entorno de ejecución (local)

| Ítem                          | Valor observado                                                                                                                                                                                    |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Fecha                         | 2026-09-09                                                                                                                                                                                         |
| Rama                          | `cursor/wp-0-baseline-2026-09-09` (desde `main`)                                                                                                                                                   |
| Node (shell)                  | v22.22.1 / también observado v26.6.0 según PATH en sesión full                                                                                                                                     |
| `engines.node` (package.json) | `20.x`                                                                                                                                                                                             |
| `.nvmrc`                      | `20`                                                                                                                                                                                               |
| pnpm                          | 11.21.0                                                                                                                                                                                            |
| Nota                          | WARN `Unsupported engine: wanted 20.x`. Build Vercel adapter avisó Node local 26 → runtime fallback 18 en bundling local. **No verificable** el runtime exacto en Production Vercel sin panel/CLI. |

## Versiones declaradas (package.json)

| Paquete                | Rango   |
| ---------------------- | ------- |
| astro                  | ^4.16.0 |
| @astrojs/vercel        | ^7.8.2  |
| react / react-dom      | ^18.3.1 |
| nodemailer             | ^6.9.15 |
| zod                    | ^3.23.8 |
| @upstash/ratelimit     | ^2.0.4  |
| @upstash/redis         | ^1.34.3 |
| @vercel/analytics      | ^2.0.1  |
| @vercel/speed-insights | ^2.0.0  |
| vitest                 | ^5.0.0  |
| @playwright/test       | ^1.48.0 |
| @lhci/cli              | ^0.14.0 |
| typescript             | ^5.6.3  |

## Scripts (`package.json`) y resultados

| Script           | Existe  | Resultado        | Notas                                                                                     |
| ---------------- | ------- | ---------------- | ----------------------------------------------------------------------------------------- |
| `dev`            | sí      | SKIPPED          | Servidor largo; no requerido para baseline                                                |
| `build`          | sí      | PASS             | `astro check` 0 errores + `astro build` OK (`ASTRO_TELEMETRY_DISABLED=1` / permisos home) |
| `preview`        | sí      | PASS (indirecto) | Usado por e2e y LHCI vía `serve .vercel/output/static`                                    |
| `typecheck`      | sí      | PASS             | `tsc --noEmit`                                                                            |
| `lint`           | sí      | PASS             | `eslint .`                                                                                |
| `format`         | sí      | SKIPPED          | Escritura; no necesario                                                                   |
| `format:check`   | sí      | PASS             | Prettier                                                                                  |
| `test`           | sí      | PASS             | 1 file / 4 tests (contactSchema)                                                          |
| `gen:og`         | sí      | SKIPPED          | Artefacto OG ya en repo; no regenerado                                                    |
| `test:e2e`       | sí      | PASS             | 7 tests (axe 6 rutas + contact mock)                                                      |
| `lhci`           | **no**  | N/A              | CI ejecuta `pnpm exec lhci autorun --config=./lighthouserc.json`                          |
| LHCI (equiv. CI) | vía CLI | PASS             | `pnpm exec lhci autorun` exit 0 en local                                                  |

`pnpm install --frozen-lockfile`: PASS.

## Workflows GitHub

| Workflow | Archivo                         | Rol                                                                                            |
| -------- | ------------------------------- | ---------------------------------------------------------------------------------------------- |
| CI       | `.github/workflows/ci.yml`      | push `main` + PR: install → typecheck → lint → format:check → test → build → playwright → LHCI |
| Release  | `.github/workflows/release.yml` | tags/releases; **no** deploy Vercel                                                            |

**Carencia / riesgo:** `release.yml` escucha `workflows: ['CI — typecheck + build + a11y + lhci']` pero el `name` de `ci.yml` es `CI — typecheck + lint + format + vitest + build + a11y + lhci`. Posible desalineación de `workflow_run` (**verificable en código; efecto en GitHub no ejecutado aquí**).

## Mapa de rutas

### Públicas (prerender / estáticas)

| Ruta                                 | Fuente                             |
| ------------------------------------ | ---------------------------------- |
| `/`                                  | `src/pages/index.astro`            |
| `/servicios`                         | `src/pages/servicios/index.astro`  |
| `/servicios/produccion-sitios-web`   | `src/pages/servicios/[slug].astro` |
| `/servicios/auditorias`              | idem                               |
| `/servicios/consultoria-tecnologica` | idem                               |
| `/proyectos`                         | `src/pages/proyectos/index.astro`  |
| `/proyectos/front-valencia`          | `src/pages/proyectos/[slug].astro` |
| `/proyectos/graficas-nasve`          | idem                               |
| `/proyectos/vcf-cribador`            | idem                               |
| `/proyectos/alexendros-me`           | idem                               |
| `/sobre-mi`                          | `src/pages/sobre-mi.astro`         |
| `/contacto`                          | `src/pages/contacto.astro`         |
| `/aviso-legal`                       | `src/pages/aviso-legal.astro`      |
| `/privacidad`                        | `src/pages/privacidad.astro`       |

Redirect: `www.alexendros.dev` → `https://alexendros.dev/:path*` (`vercel.json`, permanent).

### API

| Método / ruta       | Runtime                          | Notas                                         |
| ------------------- | -------------------------------- | --------------------------------------------- |
| `POST /api/contact` | Serverless (`prerender = false`) | Única ruta API; Astro hybrid + adapter Vercel |

Isla cliente: `ContactForm.tsx` (`client:load`) en `/contacto`.

## Contrato actual `POST /api/contact`

Fuente: `src/pages/api/contact.ts` + `src/lib/contactSchema.ts` + `ContactForm.tsx`.

### Request

| Aspecto           | Contrato actual                                                                                                                                     |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Método            | Solo export `POST` (otros métodos: comportamiento Astro/Vercel por defecto; **sin** `405` + `Allow` explícito)                                      |
| Content-Type      | Esperado JSON; **sin** chequeo 415; `request.json()` falla → body `null` → 400 Zod                                                                  |
| Body (Zod)        | `name` string trim 2–100; `email` email; `company` opcional ≤100; `subject` 1–200; `message` 20–2000; `consent` literal `true`; `honeypot` opcional |
| Destinatario SMTP | Fijo servidor: From/To `operaciones@alexendros.dev`; cliente **no** elige destinatario                                                              |
| Rate limit        | Upstash sliding window 10 / 1 min, prefijo `alexendros:contact`, clave = IP (`x-forwarded-for` primer hop o `x-real-ip`)                            |
| Honeypot          | Si relleno → **200** `{ ok: true }` (sin enviar correo)                                                                                             |

### Respuestas HTTP observadas en código

| Caso                         | Status  | Body (resumen)                                                        |
| ---------------------------- | ------- | --------------------------------------------------------------------- |
| Validación fallida           | 400     | `{ error: <Zod flatten> }` — **expone paths/detalle Zod**             |
| Honeypot                     | 200     | `{ ok: true }`                                                        |
| Rate limited                 | 429     | `{ error: 'Too many requests' }` — **sin** `Retry-After`              |
| SMTP host/user/pass ausentes | **500** | `{ error: 'Service unavailable' }` + log `contact_smtp_misconfigured` |
| Envío OK                     | 200     | `{ ok: true }`                                                        |
| Error Nodemailer             | **500** | `{ error: 'Send failed' }` + log `contact_smtp_error`                 |
| Upstash no configurado       | —       | **Fail-open**: se omite rate limit y se continúa                      |

`SMTP_PORT`: `Number(env \|\| 587)`; no valida entero explícitamente (NaN posible si valor basura).

### UI (`ContactForm`)

- `fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body })`
- Estados: idle / loading / ok / error
- Éxito: mensaje + enlace Cal.com; **no** dispara evento Analytics `contact_form_success`
- Error: mensaje genérico + pistas de campos

## `vercel.json`

- `framework`: astro
- Redirects: www → apex
- Headers globales: HSTS, nosniff, Referrer-Policy, Permissions-Policy, X-Frame-Options DENY, CSP (incl. `va.vercel-scripts.com`, vitals)
- **Sin** `rewrites`, `regions`, ni `functions` explícitos en este fichero (`maxDuration: 10` está en `astro.config.mjs` adapter)

## Matriz de variables de entorno

Valores **nunca** incluidos. Fuentes: `.env.example`, `src/env.d.ts`, uso en `contact.ts` / site.

| Nombre                     | Obligatoria*                                    | Entornos                      | Uso                     | Secreto               |
| -------------------------- | ----------------------------------------------- | ----------------------------- | ----------------------- | --------------------- |
| `SMTP_HOST`                | sí (para envío)                                 | local / Production / Preview† | server                  | no (host)             |
| `SMTP_PORT`                | recomendada (default 587)                       | idem                          | server                  | no                    |
| `SMTP_USER`                | sí (para envío)                                 | idem                          | server                  | sí (credencial)       |
| `SMTP_PASS`                | sí (para envío)                                 | idem                          | server                  | sí                    |
| `UPSTASH_REDIS_REST_URL`   | sí para rate limit (hoy opcional por fail-open) | idem                          | server                  | sí (URL de instancia) |
| `UPSTASH_REDIS_REST_TOKEN` | sí para rate limit                              | idem                          | server                  | sí                    |
| `PUBLIC_SITE_URL`          | opcional documentada                            | idem                          | client-safe (`PUBLIC_`) | no                    |

\*Obligatoria para cumplir go-live del formulario. Sin SMTP el endpoint responde 500 hoy.  
†Estado real en Vercel Production/Preview: **no verificable** en esta auditoría (no se ejecutó `vercel env ls`; no se imprimen secretos).

## Carencias observadas (para WP siguientes)

1. SMTP misconfig / send fail → HTTP **500** (objetivo WP-1/2: **503** genérico).
2. Rate limit **fail-open** si faltan `UPSTASH_*` (objetivo WP-2: fail-closed 503).
3. Sin 405 / 415 / 413 / `Retry-After` / correlation id en logs.
4. Respuesta 400 con `flatten()` Zod (detalle interno al cliente).
5. Sin tests unitarios del handler API (solo schema + e2e mock).
6. Sin evento cliente `contact_form_success` (Analytics presente en Layout).
7. Sin script npm `lhci` (solo invocación CI).
8. `engines.node` anclado a 20.x (issue #11 / WP-6).
9. Majors Dependabot diferidos (issue #12 / WP-7).
10. Posible mismatch nombre workflow CI ↔ `release.yml` `workflow_run`.
11. Env SMTP/Upstash en Vercel pendientes (issue #13) — **causa raíz** de fallo contacto en prod.

## Datos explícitamente no verificables aquí

- Contenido real de variables en Vercel (Production/Preview).
- Runtime Node efectivo en Production.
- Recepción real de correo en `operaciones@alexendros.dev`.
- Eventos runtime `contact_smtp_misconfigured` en logs Vercel (citados por issue #13 / plan; no re-consultados en esta sesión).
- Deployment Protection / SSO de previews (decisión 2A documentada en AGENTS; no inspeccionada vía API).
