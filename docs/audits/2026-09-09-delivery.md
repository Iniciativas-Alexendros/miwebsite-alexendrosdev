# Entrega Preview / Production — 2026-09-09 (WP-5)

## Modelo verificado en repo

| Aspecto               | Estado                                                                                         |
| --------------------- | ---------------------------------------------------------------------------------------------- |
| Git link Vercel       | Proyecto `alexendros-dev` ← este repo (documentado README/AGENTS)                              |
| Production branch     | `main`                                                                                         |
| Preview               | Automático por PR (Hobby + repo público, decisión 1B)                                          |
| Deployment Protection | Relajada / sin SSO en previews (decisión 2A; **panel Vercel no re-verificado en esta sesión**) |
| Promote               | Merge a `main` tras CI verde                                                                   |
| `release.yml`         | Tags/releases solamente; no dispara deploy Vercel                                              |
| `vercel.json`         | Redirect www→apex + headers seguridad/CSP; sin rewrites                                        |

## Corrección aplicada

`release.yml` `workflow_run.workflows` alineado al `name` real de `ci.yml` (`CI — typecheck + lint + format + vitest + build + a11y + lhci`). Antes no coincidía y el release automático tras CI en `main` podía no dispararse.

## Checklist operativo

1. Abrir PR desde `cursor/…` (no push a `main`).
2. Esperar Preview URL + CI verde.
3. Revisar diff; smoke contacto solo si Preview tiene sandbox SMTP/Upstash (si no, 503 fail-closed esperado).
4. Merge a `main` → Production deploy Vercel.
5. Tags vía `release.yml` / dispatch cuando toque versionar (independiente del deploy).
